import Layout from '../components/Layout';

function UserDashboard() {
    return (
        <Layout>
            <div>Kullanıcı Paneli (geçici)</div>
        </Layout>
    );
}

export default UserDashboard;